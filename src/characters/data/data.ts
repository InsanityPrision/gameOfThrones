import tyrion from "../adviser/data/data";
import { jaime, daenerys } from "../fighter/data/data";
import joffrey from "../king/data/data";
import bronn from "../squire/data/data";
import { type Character } from "../types";

export const characters: Character[] = [
  joffrey,
  jaime,
  daenerys,
  tyrion,
  bronn,
];
